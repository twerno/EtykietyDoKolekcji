import styled from 'styled-components';

export const Typ20Container = styled.div`
    width: 35mm;
    height: 40mm;
    border: 1px #cecece solid;
    overflow: hidden;

    padding: 0px;
    margin: 10px;

    position: relative;
    display: flex;
`;
